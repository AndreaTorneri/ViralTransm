#!/bin/bash -l
#PBS -l nodes=1:ppn=20:ivybridge
#PBS -l walltime=23:59:00

module load R/3.6.0-intel-2018a

out=$VSC_SCRATCH/antiviral/

wd=$out
cores=20

soft=~/projects/ViralTransm

Rscript ${soft}/src/MainVSC.R wd ${cores} ${scen} ${rho} ${prop} ${eta} ${rq} ${tfailtest} ${Ra} ${Rs} > ${out}/${id}.out 2> ${out}/${id}.err
